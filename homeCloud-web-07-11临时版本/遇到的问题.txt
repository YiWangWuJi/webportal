1、只要一调用获取dhcp配置信息的接口，就提示header中没有access-control-allow-origin，不能跨域，并且别的接口也用不了了
2、保存lan设置的接口，没有传ip地址和dhcp服务器选项参数的地方(没找到用哪个接口)
3、获取wifi物理层信息接口调用没反应,无法返回
4、获取wifi配置信息接口返回的参数与文档里的不一致，文档的是ssid，hiddenSsid，algorithms，key，实际接口返回的是ssid，hidden，key，encryption,并且文档没有指明encryption的可选值
5、设置wifi配置信息接口加token无法调用，不加token提示不可跨域



1、接口2.1.1设置WAN连接信息报文，调用提示不能跨域，并且其他的接口也不能用了
2、接口2.1.2获取WAN连接信息，PPPoE类型，没有dns返回值，没有自定义dns是否开启的返回值
3、获取lan接口基本信息，没有返回dhcp状态，无法判断并回显
4、接口2.8.3获取wifi配置信息返回的encryption的可选值和实际返回值不一样
5、接口2.8.4设置wifi物理层信息，参数beacon、aspd、shortgi、wmm不明白是代表的什么意思
6、接口2.8.3获取wifi配置信息，返回值没有表示2G或5G是否开启状态的参数
7、没有获取路由器登录密码的接口
8、接口2.8.23获取版本信息，调用提示找不到此method
9、接口2.2.1获取lan接口基本信息，返回值没有表示dhcp是否开启的参数
10、接口2.2.1获取lan接口基本信息，返回值没有表示租赁时长的参数
11、wifi接口中没有找到返回网卡mac地址的接口